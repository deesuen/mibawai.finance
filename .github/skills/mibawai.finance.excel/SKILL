---
name: mibawai.finance.excel
description: 根据指令读取 excel 财务报表，输出门店经营与明细对比分析
---
## 编码与输出规范
- 所有输出和写入文件的环节，不限于 PowerShell、Python、Node、脚本或工具调用，必须统一使用 UTF-8。
- 运行完成后必须回读 `.github/doc/` 中生成的文件，确认中文标题、表格、结论均正常显示，不得包含 `??`、`????`、方块字或其他乱码。
- 如果任何一段内容出现乱码，必须立即重写该段，直到中文正常为止，不得在乱码基础上继续分析。
- 生成结果必须保持简体中文原文，不得用英文、拼音或 `??` 替代中文。
- 写文件后必须做一次内容自检：如果文件中出现 `??`，视为生成失败，必须重新输出。


# 背景
我们经营一家火锅店，目前共有三家门店。你的主要任务是分析财务报表，并把结果写入 `.github/doc/` 下对应文件。
你需要处理的范围是：`坂田`、`福田`、`宝安` 三个门店最新年份的数据。

# 路径说明
- `.github/zhishi/`：知识库目录，只放技术说明和运行沉淀，不放模板。
- `.github/templates/`：模板目录，只放固定输出模板，不放运行沉淀。
- `.github/doc/`：输出目录，所有分析结果都写到这里。
- `excel/`：原始财务报表目录。
- `.github/columns/columns.md`：sheet 名称归类表，用于快速判断 sheet 类型。

# 读取文件前必须遵守
- 首先先执行 `rg --files .github/zhishi`，确认当前知识库里实际存在的文件名，再决定读取顺序。
- 然后执行 `rg --files .github/templates`，确认模板文件是否存在，再决定读取顺序。
- 先读取“知识库文件”，再读取“模板文件”，两者不能混为一类，禁止把模板内容当成背景知识总结。
- 知识库文件按“文件类型优先级”一次性读取，禁止把文件名写死：
  1. 先读 sheet 归类类文件
  2. 再读全局运行知识类文件
  3. 再读门店专项知识类文件
  4. 如果还有与当前任务相关的其他知识库文件，按文件名关键词匹配后一次性读取
- 具体做法是先用 `rg --files .github/zhishi | rg '关键词'` 定位，再用 `Get-Content '<命中的文件>' -Encoding UTF8` 读取，禁止一个命令一个命令地试文件名。
- 模板文件最后单独读取，仅用于固定输出格式，不作为知识库内容：
-  1. `Get-Content '.github/templates/readExcel_market_compare_template.md' -Encoding UTF8`
- 如果后续还需要找别的知识库文件，先用 `rg --files .github/zhishi | rg '关键词'` 精准定位，再读取，不要猜文件名。
- 如果后续还需要找别的模板文件，先用 `rg --files .github/templates | rg '关键词'` 精准定位，再读取，不要猜文件名。
- 所有读取命令都必须显式写出 `-Encoding UTF8`。

# 必须遵守
- 密码统一为 `88888888`
- 重点做明细分析，不要先输出 sheet 结构说明，也不要先列 sheet 名称
- 如果用户要求“明细分析 + 珠三角公开市场比价”，输出必须优先包含：
  1. 月度订单额、营业收入、平台费、食材成本、人工成本对比
  2. 每月异常点与原因判断
  3. 食材占比、人工占比、平台费占比
  4. 食材明细与珠三角公开市场价格对比
  5. 日常消耗品明细与广东/珠三角公开参考价对比
  6. 高于市场价的项目、差异金额、差异比例
  7. 供应商优化建议
  8. 是否建议继续经营，以及理由
  9. 下月行动建议
- 若公开市场价格缺失或规格不清楚，必须明确说明，不得猜测或擅自换算。
- 禁止先输出 sheet 结构、sheet 名称、目录说明。

# 固定执行顺序
1. 先抽取门店指定月份的经营汇总数据
2. 再拆解成本结构，重点看食材、人工、平台费
3. 再抽取高频食材明细，整理单价、金额、供应商
4. 再抽取日常消耗品比价页，重点看后厨用品、楼面用品、设备及餐具、清洁剂等
5. 再查珠三角/广东公开市场参考价，只能使用可核实来源
6. 再逐项对比，列出高于市场价的项目
7. 最后给出供应商建议、是否继续经营、下月行动建议

# 模板要求
- 必须先读取 `.github/templates/readExcel_market_compare_template.md`
- 该模板是明细分析的固定输出格式，不是可选参考
- 该模板属于输出模板，不属于背景知识，不要把它和知识库说明混在一起
- 若模板与其他说明冲突，以模板为准
- 如需做“明细分析 + 珠三角公开市场比价”，不要先输出 sheet 结构

# 指令支持
- `/all`：分析 excel 下全部文件
- `/all.${year}`：分析 excel 下包含该年份的全部报表
- `/only.${fileName}`：只分析单个文件
- `/only.${region}.${year}`：只分析指定门店和年份，比如 `坂田`、`福田`、`宝安`

# 输出要求
- 运行完成后，把结果写入 `.github/doc/`
- 每家门店单独生成对应文档
- 在知识库目录 `.github/zhishi/` 下补充技术层面的说明，记录这次运行时所思考的内容，运行的指令等，方便下次执行时浪费不必要的时间，不记录门店经营敏感数据
- 不要上传 git
- 不要把模板当成可选项
- 如果市场价缺失，直接说明缺口
